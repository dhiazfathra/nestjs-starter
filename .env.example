# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

DATABASE_URL=postgresql://user:password@localhost:5432/database-name?schema=public

# JWT Configuration
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRATION=1d

# Application
# For local development, use 3000
# For Docker environment, use 3001 to avoid port conflicts
PORT=3001

# Redis Cache Configuration
# For local development, use localhost
# For Docker environment, use 'redis' (the service name in docker-compose)
REDIS_HOST=localhost
# For local development with Redis installed locally, use 6379
# For Docker environment, use 6380 to avoid port conflicts
REDIS_PORT=6380
CACHE_TTL=300

# Codecov
CODECOV_TOKEN=

# Snyk
SNYK_TOKEN=

# Docker PostgreSQL Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=nestjs
# For local development with PostgreSQL installed locally, use 5432
# For Docker environment, use 5433 to avoid port conflicts
POSTGRES_PORT=5433
